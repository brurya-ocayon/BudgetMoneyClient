import { BoolConvertPipe } from './bool-convert.pipe';

describe('BoolConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
