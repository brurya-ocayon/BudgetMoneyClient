import { SortArrPipeManager } from './sort-arrMennager.pipe'

describe('sort-arrManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new SortArrPipeManager();
    expect(pipe).toBeTruthy();
  });
});
