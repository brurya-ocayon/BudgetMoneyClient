import { SortActiveUserPipe } from './sort-active-user.pipe';

describe('SortActiveUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SortActiveUserPipe();
    expect(pipe).toBeTruthy();
  });
});
