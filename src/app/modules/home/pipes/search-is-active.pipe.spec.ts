import { SearchIsActivePipe } from './search-is-active.pipe';

describe('SearchIsActivePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchIsActivePipe();
    expect(pipe).toBeTruthy();
  });
});
