import { BalanceAllTithesPipe } from './balance-all-tithes.pipe';

describe('BalanceAllTithesPipe', () => {
  it('create an instance', () => {
    const pipe = new BalanceAllTithesPipe();
    expect(pipe).toBeTruthy();
  });
});
