import { BalanceCalculationPipe } from './balance-calculation.pipe';

describe('BalanceCalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new BalanceCalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
