import { SortArrIsActivePipe } from './sort-arr-is-exist.pipe';

describe('SortArrIsActivePipe', () => {
  it('create an instance', () => {
    const pipe = new SortArrIsActivePipe();
    expect(pipe).toBeTruthy();
  });
});
