import { TitheToMonthPipe } from './tithe-to-month.pipe';

describe('TitheToMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new TitheToMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
