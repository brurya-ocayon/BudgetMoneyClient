import { TimeCalculationPipe } from './time-calculation.pipe';

describe('TimeCalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeCalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
