import { BalanceToTithePipe } from './balance-to-tithe.pipe';

describe('BalanceToTithePipe', () => {
  it('create an instance', () => {
    const pipe = new BalanceToTithePipe();
    expect(pipe).toBeTruthy();
  });
});
